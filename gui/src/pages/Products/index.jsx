import React, {Component} from "react";
import {Container} from "react-bootstrap";

class Products extends Component {
    render() {
        return <Container><span>CRUD productos</span></Container>
    }
}

export default Products;